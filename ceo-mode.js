(()=>{const ROT='kdpCEORotation';function read(k,d){try{return JSON.parse(localStorage.getItem(k)||JSON.stringify(d))}catch(e){return d}}function key(t){return t?String(t.id||t.bookId+'|'+t.url):''}function build(){let books=window.KDPProjectStore?KDPProjectStore.list():[],fc=window.KDPProductionForecast?KDPProductionForecast.all():[],q=window.KDPTodayQueue?(KDPTodayQueue.plan?KDPTodayQueue.plan(100):KDPTodayQueue.build(100)):[],scores=books.map(b=>{let f=fc.find(x=>String(x.bookId)===String(b.id))||{},tasks=q.filter(t=>String(t.bookId)===String(b.id)),score=0,reasons=[];if(f.key==='late'){score+=100;reasons.push('projekt opóźniony')}else if(f.key==='risk'){score+=75;reasons.push('zagrożony termin')}else if(f.key==='ontime')score+=25;if((f.remaining||99)<=2){score+=35;reasons.push('blisko publikacji')}if(tasks.some(t=>t.rescuePriority==='urgent'||t.priorityKey==='urgent')){score+=40;reasons.push('pilne zadanie')}if(f.publication&&Number.isFinite(f.days))score+=Math.max(0,30-f.days);return{bookId:b.id,title:b.title||'Bez tytułu',score,reasons,status:f.status||'Brak terminu',remaining:f.remaining==null?'—':f.remaining,publication:f.publication||'',task:tasks[0]||null,tasks}}).filter(x=>x.task||x.status!=='Brak terminu').sort((a,b)=>b.score-a.score),focus=scores.slice(0,3),defer=scores.slice(3),rot=read(ROT,{history:[]});let out={createdAt:new Date().toISOString(),focus,defer,total:scores.length,rotationHistory:rot.history||[]};localStorage.setItem('kdpCEOPlan',JSON.stringify(out));return out}function rotate(currentBookId){let p=build(),focusIds=p.focus.map(x=>String(x.bookId)),replacement=p.defer.find(x=>x.task&&!focusIds.includes(String(x.bookId))),history=read(ROT,{history:[]}).history||[];if(replacement){history.unshift({at:new Date().toISOString(),out:String(currentBookId||''),in:String(replacement.bookId),title:replacement.title});history=history.slice(0,50);localStorage.setItem(ROT,JSON.stringify({history,last:history[0]}));return replacement}return null}function nextFocus(excludeIds=[]){let p=build(),ex=new Set(excludeIds.map(String));return [...p.focus,...p.defer].find(x=>x.task&&!ex.has(String(x.bookId)))||null}function open(x){if(!x||!x.task)return false;if(window.KDPTodayQueue&&KDPTodayQueue.openTask)return KDPTodayQueue.openTask(x.bookId,x.task.url);location.href=x.task.url||'production-manager.html'}window.KDPCEOMode={build,open,rotate,nextFocus,rotation:()=>read(ROT,{history:[]})};})();