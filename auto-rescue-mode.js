(()=>{function read(k,d){try{return JSON.parse(localStorage.getItem(k)||JSON.stringify(d))}catch(e){return d}}function local(d=new Date()){return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0')}function rescue(bookId){if(!window.KDPProductionForecast)return null;let f=KDPProductionForecast.all().find(x=>String(x.bookId)===String(bookId));if(!f||!['risk','late'].includes(f.key))return null;let all=read('kdpScheduledWork',[]),mine=all.filter(t=>String(t.bookId)===String(bookId)&&!t.done).sort((a,b)=>String(a.at||'').localeCompare(String(b.at||''))),today=local(),take=Math.min(3,Math.max(1,Math.ceil(mine.length/Math.max(1,f.days||1))));mine.slice(0,take).forEach((t,i)=>{t.at=today+'T'+String(9+i*2).padStart(2,'0')+':00';t.rescue=true;t.rescuePriority='urgent';t.rescueAt=new Date().toISOString()});localStorage.setItem('kdpScheduledWork',JSON.stringify(all));let plan={bookId:f.bookId,title:f.title,status:f.status,createdAt:new Date().toISOString(),today:mine.slice(0,take).map(t=>({id:t.id,text:t.type||t.stage||'Zadanie',url:t.url||'production-manager.html'})),remaining:Math.max(0,mine.length-take),publication:f.publication};localStorage.setItem('kdpRescuePlan:'+bookId,JSON.stringify(plan));localStorage.setItem('kdpLastRescuePlan',JSON.stringify(plan));return plan}function rescueAll(){return KDPProductionForecast.all().filter(x=>['risk','late'].includes(x.key)).map(x=>rescue(x.bookId)).filter(Boolean)}window.KDPAutoRescue={rescue,rescueAll,get:id=>read('kdpRescuePlan:'+id,null)};})();