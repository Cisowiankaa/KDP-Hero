(()=>{const STEPS=[['keywords','keywords.html'],['outline','outline.html'],['writer','writer.html'],['manuscript','manuscript.html'],['cover','cover.html'],['check','check.html'],['package','publish-package.html'],['publishing','publishing-center.html']];function get(k,d={}){try{return JSON.parse(localStorage.getItem(k)||JSON.stringify(d))}catch(e){return d}}function state(){let k=get('kdpKeywords'),o=get('kdpOutline'),w=get('kdpWriter'),m=get('kdpManuscript'),c=get('kdpCover'),ch=get('kdpPublishReady'),p=get('kdpPublishPackage'),f=get('kdpFinalPublishing'),keys=Array.isArray(k.selected)?k.selected:(Array.isArray(k.keywords)?k.keywords:[]),chap=Array.isArray(w.chapters)?w.chapters:[],done={keywords:keys.length>=3,outline:!!(o.chapters?.length||chap.length),writer:chap.length>0&&chap.every(x=>String(x.text||'').trim()),manuscript:String(m.text||m.content||'').trim().length>500,cover:!!(c.generated||c.prompt||c.variant),check:!!ch.ready,package:!!String(p.description||'').trim(),publishing:!!f.ready};let first=STEPS.find(([id])=>!done[id]),next=first?first[1]:'publishing-center.html',completed=Object.values(done).filter(Boolean).length,progress=Math.round(completed/STEPS.length*100);return{done,next,completed,total:STEPS.length,progress,updatedAt:new Date().toISOString()}}function save(){let s=state();localStorage.setItem('kdpWorkflow',JSON.stringify(s));if(window.KDPProjectStore)KDPProjectStore.saveCurrent();return s}function next(){return save().next}window.KDPWorkflow={state,save,next,STEPS};document.addEventListener('DOMContentLoaded',save);window.addEventListener('pagehide',save)})();