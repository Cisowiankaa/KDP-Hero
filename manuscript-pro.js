(()=>{const $=s=>document.querySelector(s);function get(k){try{return JSON.parse(localStorage.getItem(k)||'{}')||{}}catch(e){return {}}}function install(){let main=document.querySelector('main');if(!main||$('#manuscriptPro'))return;let sec=document.createElement('section');sec.id='manuscriptPro';sec.className='card';sec.innerHTML=`<h3>Manuskrypt Pro</h3><p class="muted">Dodaj profesjonalne strony początkowe i uporządkuj strukturę książki.</p><div style="display:grid;grid-template-columns:repeat(3,1fr);gap:10px"><label>Rok wydania<input id="proYear" class="field" style="width:100%;margin-top:5px" value="${new Date().getFullYear()}"></label><label>Wydawca / marka<input id="proPublisher" class="field" style="width:100%;margin-top:5px" placeholder="Self-published"></label><label>ISBN<input id="proIsbn" class="field" style="width:100%;margin-top:5px" placeholder="opcjonalnie"></label></div><label style="display:block;margin-top:10px">Dedykacja<input id="proDedication" class="field" style="width:100%;margin-top:5px" placeholder="opcjonalnie"></label><p><button id="proBuild" class="btn">✦ Zbuduj wersję PRO</button></p><div id="proStatus" class="ok"></div>`;let editor=[...main.querySelectorAll('section.card')].find(x=>x.querySelector('#text'));main.insertBefore(sec,editor||main.lastElementChild);$('#proBuild').onclick=build}function build(){let w=get('kdpWriter'),m=get('kdpManuscript'),title=w.title||'Bez tytułu',author=w.author||'Karolcia',year=$('#proYear').value||new Date().getFullYear(),publisher=$('#proPublisher').value.trim()||'Self-published',isbn=$('#proIsbn').value.trim(),ded=$('#proDedication').value.trim(),chs=Array.isArray(w.chapters)?w.chapters:[];if(!chs.length){$('#proStatus').textContent='Najpierw utwórz rozdziały w Auto Writerze.';return}let intro=chs.find(x=>/wprowadzenie/i.test(x.title||'')),ending=chs.find(x=>/zakończenie/i.test(x.title||'')),body=chs.filter(x=>!/wprowadzenie|zakończenie/i.test(x.title||''));let copyright=`© ${year} ${author}\nWszelkie prawa zastrzeżone.\nWydawca: ${publisher}${isbn?'\nISBN: '+isbn:''}\n\nŻadna część tej publikacji nie może być powielana ani rozpowszechniana bez zgody właściciela praw, z wyjątkiem przypadków dozwolonych przez prawo.`;let toc='SPIS TREŚCI\n\n'+['Wprowadzenie',...body.map((x,i)=>`Rozdział ${i+1}. ${x.title}`),'Zakończenie'].join('\n');let parts=[title.toUpperCase(),author,'',copyright];if(ded)parts.push('DEDYKACJA\n\n'+ded);parts.push(toc);if(intro)parts.push('WPROWADZENIE\n\n'+intro.text);body.forEach((x,i)=>parts.push(`ROZDZIAŁ ${i+1}\n${x.title}\n\n${x.text}`));if(ending)parts.push('ZAKOŃCZENIE\n\n'+ending.text);let text=parts.join('\n\n\n──────────\n\n\n');m={...m,title,author,content:text,text,frontMatter:{year,publisher,isbn,dedication:ded},chapters:chs};localStorage.setItem('kdpManuscript',JSON.stringify(m));$('#text').value=text;$('#text').dispatchEvent(new Event('input'));$('#proStatus').textContent='✓ Wersja PRO gotowa: strona tytułowa, copyright, spis treści i uporządkowane rozdziały.'}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install()})();