(()=>{const MAX=4;function read(k,d){try{return JSON.parse(localStorage.getItem(k)||JSON.stringify(d))}catch(e){return d}}function local(d){return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0')}function day(s){return new Date(String(s).slice(0,10)+'T12:00')}function isWork(d){let n=d.getDay();return n!==0&&n!==6}function tasks(){return read('kdpScheduledWork',[]).filter(t=>!t.done&&t.at)}function counts(list=tasks()){let x={};list.forEach(t=>{let k=String(t.at).slice(0,10);x[k]=(x[k]||0)+1});return x}function overloads(){return Object.entries(counts()).filter(([,n])=>n>MAX).map(([date,count])=>({date,count,excess:count-MAX})).sort((a,b)=>a.date.localeCompare(b.date))}function pubDate(t){return t.publicationDate||''}function findSlot(t,used){let original=day(t.at),pub=pubDate(t)?day(pubDate(t)):null;for(let delta=1;delta<=10;delta++){for(let dir of [-1,1]){let d=new Date(original);d.setDate(d.getDate()+delta*dir);if(!isWork(d))continue;if(pub&&d>pub)continue;let k=local(d);if((used[k]||0)<MAX)return k}}return null}function balance(){let list=tasks(),all=read('kdpScheduledWork',[]),used=counts(list),moves=[];overloads().forEach(o=>{let on=list.filter(t=>String(t.at).slice(0,10)===o.date).sort((a,b)=>String(a.publicationDate||'9999').localeCompare(String(b.publicationDate||'9999')));for(let i=MAX;i<on.length;i++){let t=on[i],slot=findSlot(t,used);if(!slot)continue;let item=all.find(x=>x.id===t.id);if(!item)continue;let old=String(item.at).slice(0,10);item.at=slot+'T09:00';item.balancedFrom=old;item.balancedAt=new Date().toISOString();used[old]=Math.max(0,(used[old]||1)-1);used[slot]=(used[slot]||0)+1;moves.push({id:item.id,title:item.bookTitle||'Bez tytułu',task:item.type||item.stage||'Zadanie',from:old,to:slot})}});localStorage.setItem('kdpScheduledWork',JSON.stringify(all));localStorage.setItem('kdpLastCalendarBalance',JSON.stringify({at:new Date().toISOString(),moves}));return moves}window.KDPCalendarLoad={max:MAX,overloads,balance,counts};})();