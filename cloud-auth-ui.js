(()=>{if(window.KDPCloudAuthUI)return;const SESSION='kdpSupabaseSession';function read(){try{return JSON.parse(localStorage.getItem(SESSION)||'{}')}catch(e){return{}}}function tokenFromUrl(){let h=new URLSearchParams(location.hash.replace(/^#/,'')),q=new URLSearchParams(location.search),access=h.get('access_token')||q.get('access_token'),refresh=h.get('refresh_token')||q.get('refresh_token'),expires=Number(h.get('expires_at')||q.get('expires_at')||0);if(!access)return false;KDPCloudSync?.setSession?.({access_token:access,refresh_token:refresh||'',expires_at:expires});history.replaceState({},document.title,location.pathname);return true}function render(){let host=document.querySelector('#cloudSyncSafe');if(!host)return;let old=host.querySelector('#cloudAuthBox');if(old)old.remove();let s=read(),box=document.createElement('div');box.id='cloudAuthBox';box.style='margin-top:8px;padding-top:8px;border-top:1px solid #e5e8ee';if(s.access_token){box.innerHTML='<small style="display:block;color:#08794e">✓ Sesja chmury aktywna</small><button class="btn2" id="cloudLogout" style="margin-top:6px">Wyloguj chmurę</button>';box.querySelector('#cloudLogout').onclick=()=>{KDPCloudSync?.setSession?.(null);render()}}else{box.innerHTML='<small style="display:block;color:#70788c">Zaloguj konto, aby włączyć backup między urządzeniami.</small><button class="btn2" id="cloudLoginInfo" style="margin-top:6px">Połącz konto</button>';box.querySelector('#cloudLoginInfo').onclick=()=>window.dispatchEvent(new CustomEvent('kdp-cloud-login-requested'))}host.appendChild(box)}tokenFromUrl();document.addEventListener('DOMContentLoaded',()=>setTimeout(render,260));window.addEventListener('kdp-cloud-session-change',render);window.addEventListener('kdp-persistent-restored',render);window.KDPCloudAuthUI={render,session:read,consumeRedirect:tokenFromUrl};})();