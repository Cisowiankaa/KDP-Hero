(()=>{function get(k,d={}){try{return JSON.parse(localStorage.getItem(k)||JSON.stringify(d))}catch(e){return d}}function alerts(){let w=get('kdpWriter'),m=get('kdpManuscript'),c=get('kdpCover'),k=get('kdpKeywords'),e=get('kdpEditorReport'),check=get('kdpPublishReady'),p=get('kdpPublishPackage'),chs=Array.isArray(w.chapters)?w.chapters:[],kw=Array.isArray(k.selected)?k.selected:[],out=[];chs.forEach((x,i)=>{let n=String(x.text||'').trim().split(/\s+/).filter(Boolean).length;if(x.text&&n<500)out.push({level:'warn',title:`Rozdział ${i+1} jest za krótki`,text:`${n} słów — warto go rozwinąć.`,url:'writer.html',action:'Rozwiń rozdział'})});if(!String(m.content||m.text||'').trim())out.push({level:'high',title:'Brak kompletnego manuskryptu',text:'Zbuduj książkę z gotowych rozdziałów.',url:'manuscript.html',action:'Zbuduj manuskrypt'});if(!(c.generated||c.prompt||c.variant))out.push({level:'high',title:'Brakuje okładki',text:'Projekt nie ma jeszcze przygotowanej okładki.',url:'cover.html',action:'Przygotuj okładkę'});if(kw.length<7)out.push({level:'warn',title:`Keywords: ${kw.length}/7`,text:'Uzupełnij frazy wyszukiwania dla Amazon KDP.',url:'keywords.html',action:'Uzupełnij keywords'});if(e.score!=null&&e.score<70)out.push({level:'high',title:`Jakość tekstu: ${e.score}/100`,text:'AI Editor wykrył elementy wymagające poprawy.',url:'editor.html',action:'Popraw tekst'});if(check.score!=null&&!check.ready)out.push({level:'high',title:`KDP Check: ${check.score}%`,text:'Książka nie przeszła jeszcze wszystkich kontroli.',url:'check.html',action:'Napraw KDP Check'});if(!p.complete&&check.ready)out.push({level:'warn',title:'Publish Package niekompletny',text:'KDP Check jest gotowy — przygotuj pakiet publikacyjny.',url:'publish-package.html',action:'Zbuduj pakiet'});return out.slice(0,8)}function install(){let root=document.querySelector('#content');if(!root||document.querySelector('#alertCenter'))return;let a=alerts(),sec=document.createElement('section');sec.id='alertCenter';sec.className='card';sec.innerHTML=`<div style="display:flex;justify-content:space-between;align-items:center;gap:10px"><div><h3 style="margin:0">🔔 Centrum alertów</h3><div class="muted" style="margin-top:5px">KDP HERO wskazuje problemy i prowadzi bezpośrednio do ich naprawy.</div></div><b style="font-size:22px;color:${a.length?'#a66200':'#08794e'}">${a.length?a.length:'✓'}</b></div><div id="alertRows" style="margin-top:12px"></div>`;let live=document.querySelector('#liveBookStatus');live?live.after(sec):root.prepend(sec);let rows=sec.querySelector('#alertRows');rows.innerHTML=a.length?a.map(x=>`<div style="display:grid;grid-template-columns:12px 1fr auto;gap:12px;align-items:center;padding:12px 0;border-bottom:1px solid #edf0f4"><span style="width:10px;height:10px;border-radius:50%;background:${x.level==='high'?'#c43d4d':'#e2a13a'}"></span><div><b>${x.title}</b><div class="muted" style="font-size:12px;margin-top:3px">${x.text}</div></div><a class="btn2" href="${x.url}">${x.action} →</a></div>`).join(''):'<div style="padding:18px 0;color:#08794e;font-weight:800">✓ Brak krytycznych problemów. Projekt wygląda dobrze.</div>'}window.KDPDashboardAlerts={alerts,install};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(install,320));else setTimeout(install,320)})();