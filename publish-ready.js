(()=>{function get(k){try{return JSON.parse(localStorage.getItem(k)||'{}')||{}}catch(e){return {}}}function install(){let main=document.querySelector('main');if(!main||document.querySelector('#publishReady'))return;let sec=document.createElement('section');sec.id='publishReady';sec.className='card';sec.innerHTML='<h2>Centrum gotowości do publikacji</h2><p class="muted">Jeden końcowy wynik dla treści, okładki i eksportu.</p><div id="readyTiles" class="meta"></div><div id="readyTotal" style="font-size:38px;font-weight:900;margin:18px 0"></div><div id="readyBlockers"></div><div class="actions" style="margin-top:15px"><a href="writer.html" class="btn secondary">AI Pisanie</a><a href="cover.html" class="btn secondary">Okładka</a><a href="export.html" class="btn secondary">Eksport</a></div>';let complete=document.querySelector('#complete');main.insertBefore(sec,complete);render()}function render(){let quality=get('kdpQualityGate'),cover=get('kdpCoverCheck'),exp=get('kdpExportSettings'),base=+(localStorage.getItem('kdpCheckScore')||0),scores=[['Treść / Quality Gate',quality.score||0,quality.ready],['Okładka / Cover Check',cover.score||0,cover.ready],['KDP Check',base,base===100],['Eksport / Preflight',exp.ready?100:0,!!exp.ready]],total=Math.round(scores.reduce((a,x)=>a+x[1],0)/scores.length),tiles=document.querySelector('#readyTiles');tiles.innerHTML=scores.map(x=>`<div><small>${x[0]}</small><b style="color:${x[2]?'#08794e':'#a66200'}">${x[1]}%</b></div>`).join('');document.querySelector('#readyTotal').innerHTML=`${total}% <span style="font-size:14px;color:#747c8e">gotowości całego projektu</span>`;let blockers=scores.filter(x=>!x[2]);document.querySelector('#readyBlockers').innerHTML=blockers.length?'<b>Blokery publikacji:</b><p class="muted">'+blockers.map(x=>x[0]).join(' · ')+'</p>':'<p class="ready">✓ Wszystkie moduły zaliczone. Projekt jest gotowy do finalizacji.</p>';localStorage.setItem('kdpPublishReady',JSON.stringify({score:total,ready:blockers.length===0,modules:scores,updatedAt:new Date().toISOString()}))}window.addEventListener('storage',render);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install()})();