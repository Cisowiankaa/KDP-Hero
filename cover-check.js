(()=>{const $=s=>document.querySelector(s);function install(){let main=document.querySelector('main');if(!main||$('#coverCheck'))return;let sec=document.createElement('section');sec.id='coverCheck';sec.className='card';sec.style='margin-top:18px';sec.innerHTML=`<h2>KDP Cover Check</h2><p class="muted">Kontrola techniczna okładki przed publikacją.</p><p><button id="runCoverCheck" class="btn">✓ Sprawdź okładkę</button></p><div id="coverCheckScore" style="font-size:32px;font-weight:900"></div><div id="coverCheckList"></div><div id="coverCheckDecision" class="status"></div>`;main.appendChild(sec);$('#runCoverCheck').onclick=run;run()}function run(){let title=$('#title')?.value.trim(),author=$('#author')?.value.trim(),pages=+($('#pages')?.value||0),type=$('#type')?.value||'',fmt=$('#format')?.value||'',wrap=$('#wrapCanvas'),front=$('#coverCanvas'),back=$('#backText')?.value.trim()||'',paper=$('#paper')?.value||'white',minSpine=paper==='cream'?79:79,checks=[['Tytuł na okładce',!!title,'Uzupełnij tytuł.'],['Autor',!!author,'Uzupełnij autora.'],['Format książki',!!fmt,'Wybierz format.'],['Liczba stron',pages>=24,'Podaj poprawną liczbę stron.'],['Front 300 DPI',!!front&&front.width>=1800&&front.height>=2700,'Wygeneruj front w wysokiej rozdzielczości.'],['Full wrap',type!=='Paperback'||(!!wrap&&wrap.width>3000),'Wygeneruj pełną okładkę paperback.'],['Tekst tylnej okładki',type!=='Paperback'||back.length>=30,'Dodaj opis tylnej okładki.'],['Grzbiet',type!=='Paperback'||pages>=minSpine,pages<minSpine?'Przy tej liczbie stron tekst na grzbiecie może nie być odpowiedni.':'Gotowe.'],['Strefa kodu kreskowego',type!=='Paperback'||!!wrap,'Generator rezerwuje miejsce na kod kreskowy.'],['Spad 0.125 in',type!=='Paperback'||!!wrap,'Full wrap zawiera spad 0.125 in.']];let passed=checks.filter(x=>x[1]).length,score=Math.round(passed/checks.length*100);$('#coverCheckScore').textContent=score+'/100';$('#coverCheckScore').style.color=score===100?'#08794e':score>=70?'#a66200':'#b43b3b';$('#coverCheckList').innerHTML=checks.map(x=>`<div class="check" style="margin-top:7px">${x[1]?'✓':'!'} <b>${x[0]}</b> — ${x[1]?'OK':x[2]}</div>`).join('');$('#coverCheckDecision').textContent=score===100?'✓ Okładka przeszła KDP Cover Check.':'Okładka wymaga poprawek przed finalnym eksportem.';localStorage.setItem('kdpCoverCheck',JSON.stringify({score,ready:score===100,checks,updatedAt:new Date().toISOString()}))}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install()})();