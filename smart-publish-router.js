(()=>{function get(k,d={}){try{return JSON.parse(localStorage.getItem(k)||JSON.stringify(d))}catch(e){return d}}function next(){let gate=get('kdpQualityGate'),pre=get('kdpPrePublishStatus'),launch=get('kdpLaunchChecklist'),ready=get('kdpLaunchReady');if(ready.ready||launch.status==='live')return{url:'launch-checklist.html',label:'✓ Publikacja zakończona',stage:'published'};if(pre.ready===true)return{url:'launch-checklist.html',label:'🚀 Kontynuuj Launch Checklist',stage:'launch'};if(gate.ready===true)return{url:'pre-publish.html',label:'🚀 Kontynuuj Pre-Publish',stage:'prepublish'};return{url:'publishing-center.html',label:'🚀 Publikuj książkę',stage:'quality'}}function install(){let b=document.querySelector('#smartPublish');if(!b)return;let n=next();b.href=n.url;b.textContent=n.label;b.dataset.stage=n.stage}window.KDPSmartPublish={next,install};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install()})();