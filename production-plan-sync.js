(()=>{const STAGES=[['Research niszy i keywords','keywords.html'],['AI Outline Creator','outline.html'],['Pisanie rozdziałów','writer.html'],['Korekta i manuskrypt','manuscript.html'],['Okładka AI','cover.html'],['KDP Check i Quality Gate','check.html'],['Przygotowanie publikacji','publishing-center.html']];function read(k,d={}){try{return JSON.parse(localStorage.getItem(k)||JSON.stringify(d))}catch(e){return d}}function write(k,v){localStorage.setItem(k,JSON.stringify(v))}function addDays(s,n){let d=new Date(s+'T12:00');d.setDate(d.getDate()+n);return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0')}function sync(){let m=read('kdpMonthlyProduction'),items=Array.isArray(m.items)?m.items:[],weeklyTasks=[],scheduled=read('kdpScheduledWork',[]);items.forEach((book,bi)=>{STAGES.forEach((stage,i)=>{let date=addDays(book.start,i),id=`month-${m.month}-${book.id}-${i}`;weeklyTasks.push({id,bookId:book.id,bookTitle:book.title,stage:stage[0],url:stage[1],date,done:false,source:'monthly'});if(!scheduled.some(x=>x.id===id))scheduled.push({id,bookId:book.id,bookTitle:book.title,type:stage[0],note:'Automatycznie z planu miesięcznego',at:date+'T09:00',done:false,createdAt:new Date().toISOString(),source:'monthly'})})});write('kdpMonthlyGeneratedTasks',{month:m.month,tasks:weeklyTasks,updatedAt:new Date().toISOString()});write('kdpScheduledWork',scheduled);return{books:items.length,tasks:weeklyTasks.length}}window.KDPProductionPlanSync={sync};})();