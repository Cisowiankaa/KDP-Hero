(()=>{const KEY='kdpSmartWorkload';function read(k,d){try{return JSON.parse(localStorage.getItem(k)||JSON.stringify(d))}catch(e){return d}}function write(k,v){localStorage.setItem(k,JSON.stringify(v));return v}function local(d=new Date()){return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0')}function reports(){return read('kdpDailyReports',[]).filter(r=>r&&r.day).slice(0,14)}function capacity(){let rr=reports(),worked=rr.filter(r=>(r.completed||0)>0),avg=worked.length?worked.reduce((n,r)=>n+(r.completed||0),0)/worked.length:4,carry=rr.length?rr.reduce((n,r)=>n+(r.carried||0),0)/rr.length:0,raw=Math.round(avg-(carry*.35)),limit=Math.max(2,Math.min(8,raw||4));return{limit,average:+avg.toFixed(1),carry:+carry.toFixed(1),sample:worked.length}}function urgent(t){return t.priorityKey==='urgent'||t.rescuePriority==='urgent'||t.rescue===true||String(t.status||'').toLowerCase().includes('zaleg')}function plan(){let c=capacity(),q=window.KDPTodayQueue?(KDPTodayQueue.plan?KDPTodayQueue.plan(50):KDPTodayQueue.build(50)):[],chosen=[],seen=new Set(),ordered=[...q.filter(urgent),...q.filter(t=>!urgent(t))];ordered.forEach(t=>{let k=String(t.id||t.bookId+'|'+t.url);if(!seen.has(k)&&chosen.length<c.limit){seen.add(k);chosen.push(t)}});let result={day:local(),createdAt:new Date().toISOString(),capacity:c,available:q.length,tasks:chosen,deferred:Math.max(0,q.length-chosen.length),urgent:chosen.filter(urgent).length};write(KEY,result);return result}window.KDPSmartWorkload={capacity,plan,get:()=>read(KEY,null),isUrgent:urgent};})();