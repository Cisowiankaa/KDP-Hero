(()=>{const KEY='kdpSmartWorkload',DEFER='kdpDeferredWorkload',HISTORY='kdpDayAutopilotHistory',GUARD='kdpWorkloadGuard';function read(k,d){try{return JSON.parse(localStorage.getItem(k)||JSON.stringify(d))}catch(e){return d}}function write(k,v){localStorage.setItem(k,JSON.stringify(v));return v}function local(d=new Date()){return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0')}function nextWorkday(){let d=new Date();do{d.setDate(d.getDate()+1)}while(d.getDay()===0||d.getDay()===6);return local(d)}function reports(){return read('kdpDailyReports',[]).filter(r=>r&&r.day).slice(0,14)}function history(){let h=read(HISTORY,[]);return Array.isArray(h)?h.filter(x=>x&&x.day).slice(0,30):[]}function settings(){let s=read('kdpProductionLimits',{}),manual=Number(s.dailyTasks);return{dailyTasks:Number.isFinite(manual)&&manual>=1?Math.max(1,Math.min(20,Math.round(manual))):null,maxBooks:Number.isFinite(Number(s.maxBooks))?Math.max(1,Math.min(10,Math.round(Number(s.maxBooks)))):null}}function performance(){let h=history().filter(x=>(Number(x.completed)||0)>0).slice(0,14),r=reports().filter(x=>(Number(x.completed)||0)>0).slice(0,14),src=h.length?h:r,values=src.map(x=>Number(x.completed)||0),avg=values.length?values.reduce((a,b)=>a+b,0)/values.length:4,recent=values.slice(0,7),older=values.slice(7,14),ra=recent.length?recent.reduce((a,b)=>a+b,0)/recent.length:avg,oa=older.length?older.reduce((a,b)=>a+b,0)/older.length:ra,trend=oa?Math.round((ra-oa)/oa*100):0,limits=h.map(x=>Number(x.capacity&&x.capacity.limit)||Number(x.total)||0).filter(Boolean),util=limits.length?Math.round(h.slice(0,limits.length).reduce((n,x)=>{let l=Number(x.capacity&&x.capacity.limit)||Number(x.total)||0;return n+(l?Math.min(1,(Number(x.completed)||0)/l):0)},0)/limits.length*100):null;return{average:+avg.toFixed(1),recentAverage:+ra.toFixed(1),trend,sample:values.length,utilization:util,source:h.length?'autopilot-history':'daily-reports',values}}function burnout(perf){let v=perf.values||[],recent=v.slice(0,3),low=recent.filter(x=>x<Math.max(2,perf.average*.7)).length,declining=recent.length===3&&recent[0]<=recent[1]&&recent[1]<=recent[2]&&recent[0]<perf.average*.85,carry=reports().slice(0,3).reduce((n,r)=>n+(Number(r.carried)||0),0),active=recent.length>=3&&(low>=2||declining||carry>=4),severity=active?(low===3||carry>=7?'high':'medium'):'none';return{active,severity,lowDays:low,declining,carry,recent,reason:active?(severity==='high'?'kilka słabszych dni i narastające zaległości':'tempo spada przez kilka kolejnych dni'):'brak oznak przeciążenia'}}function guard(raw,perf){let g=read(GUARD,{}),previous=Number(g.lastRecommended)||null,candidate=Math.max(2,Math.min(10,raw||4)),maxRise=1,maxDrop=2,protectedLimit=candidate,reason='stabilny',b=burnout(perf);if(previous){if(candidate>previous+maxRise){protectedLimit=previous+maxRise;reason='wzrost ograniczony do +1 zadania/dzień'}else if(candidate<previous-maxDrop){protectedLimit=previous-maxDrop;reason='spadek ograniczony do -2 zadań/dzień'}}if(perf.sample<3&&candidate>4){protectedLimit=Math.min(protectedLimit,4);reason='mało danych — limit bezpieczeństwa 4'}if(b.active){let recovery=b.severity==='high'?Math.max(2,Math.round(perf.recentAverage)-2):Math.max(2,Math.round(perf.recentAverage)-1);protectedLimit=Math.min(protectedLimit,recovery);reason='Burnout Guard — '+b.reason}protectedLimit=Math.max(2,Math.min(10,protectedLimit));let out={day:local(),raw:candidate,recommended:protectedLimit,lastRecommended:protectedLimit,previous,reason,sample:perf.sample,burnout:b,updatedAt:new Date().toISOString()};write(GUARD,out);return out}function capacity(){let rr=reports(),perf=performance(),carry=rr.length?rr.reduce((n,r)=>n+(r.carried||0),0)/rr.length:0,base=perf.recentAverage||perf.average||4,trendBoost=perf.sample>=4?(perf.trend>12?1:perf.trend<-12?-1:0):0,utilBoost=perf.utilization!=null&&perf.sample>=4?(perf.utilization>=90?1:perf.utilization<60?-1:0):0,raw=Math.round(base-(carry*.35)+trendBoost+utilBoost),g=guard(raw,perf),auto=g.recommended,s=settings(),limit=s.dailyTasks||auto;return{limit,autoLimit:auto,recommended:auto,rawRecommended:g.raw,manual:!!s.dailyTasks,average:perf.average,recentAverage:perf.recentAverage,trend:perf.trend,utilization:perf.utilization,carry:+carry.toFixed(1),sample:perf.sample,source:perf.source,maxBooks:s.maxBooks,guard:g,burnout:g.burnout,reason:s.dailyTasks?'Limit ustawiony ręcznie':`Rekomendacja ${auto} zadań · ${g.reason} · tempo ${perf.recentAverage}/dzień${perf.utilization!=null?' · wykorzystanie '+perf.utilization+'%':''}`}}function urgent(t){return t.priorityKey==='urgent'||t.rescuePriority==='urgent'||t.rescue===true||String(t.status||'').toLowerCase().includes('zaleg')}function unique(q){let seen=new Set();return q.filter(t=>{let k=String(t.id||t.bookId+'|'+t.url);if(!k||seen.has(k))return false;seen.add(k);return true})}function applyBookLimit(tasks,maxBooks){if(!maxBooks)return tasks;let books=new Set(),out=[];tasks.forEach(t=>{let id=String(t.bookId||'');if(!id){out.push(t);return}if(books.has(id)){out.push(t);return}if(books.size<maxBooks){books.add(id);out.push(t)}});return out}function defer(tasks,c){let tomorrow=nextWorkday(),payload={day:tomorrow,createdAt:new Date().toISOString(),source:'smart-workload-cap',capacity:c.limit,tasks:tasks.map(t=>({id:t.id||'',bookId:t.bookId||'',title:t.title||t.bookTitle||'Bez tytułu',text:t.text||t.type||t.stage||'Kontynuuj produkcję',url:t.url||'production-manager.html',priorityKey:t.priorityKey||'',status:t.status||''})),total:tasks.length};write(DEFER,payload);return payload}function plan(){let c=capacity(),q=window.KDPTodayQueue?(KDPTodayQueue.plan?KDPTodayQueue.plan(100):KDPTodayQueue.build(100)):[],ordered=unique([...q.filter(urgent),...q.filter(t=>!urgent(t))]),eligible=applyBookLimit(ordered,c.maxBooks),chosen=eligible.slice(0,c.limit),chosenKeys=new Set(chosen.map(t=>String(t.id||t.bookId+'|'+t.url))),deferred=ordered.filter(t=>!chosenKeys.has(String(t.id||t.bookId+'|'+t.url))),next=defer(deferred,c),result={day:local(),createdAt:new Date().toISOString(),capacity:c,available:q.length,tasks:chosen,deferred:deferred.length,deferredPlan:next,urgent:chosen.filter(urgent).length,books:[...new Set(chosen.map(t=>String(t.bookId||'')).filter(Boolean))].length};write(KEY,result);return result}function setLimits(dailyTasks,maxBooks){let s={dailyTasks:dailyTasks==null?null:Math.max(1,Math.min(20,Math.round(Number(dailyTasks)||1))),maxBooks:maxBooks==null?null:Math.max(1,Math.min(10,Math.round(Number(maxBooks)||1))),updatedAt:new Date().toISOString()};write('kdpProductionLimits',s);window.dispatchEvent(new CustomEvent('kdp-workload-limit-change',{detail:s}));return s}window.KDPSmartWorkload={capacity,performance,burnout,plan,get:()=>read(KEY,null),deferred:()=>read(DEFER,null),guard:()=>read(GUARD,null),isUrgent:urgent,settings,setLimits,nextWorkday};})();