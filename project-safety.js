(()=>{function get(k,d={}){try{return JSON.parse(localStorage.getItem(k)||JSON.stringify(d))}catch(e){return d}}function status(){let recovery=get('kdpWorkflowRecovery'),backups=get('kdpAutoBackups',[]),final=get('kdpFinalPublishing'),workflow=get('kdpWorkflow'),last=backups[0]||null;return{recovery:{active:!!recovery.at,next:recovery.next||null,completed:recovery.completed||0,total:recovery.total||10,last:recovery.at||null},backup:{active:true,count:backups.length,last:last?.at||null,reason:last?.reason||null},workflow:{progress:workflow.progress||0,next:workflow.next||recovery.nextUrl||'keywords.html'},publishing:{ready:!!final.ready,score:final.score||0},healthy:!!last&&!!recovery.at}}function checkpoint(reason='safety'){if(window.KDPRecovery)KDPRecovery.checkpoint(reason);if(window.KDPAutoBackup)KDPAutoBackup.snapshot(reason);return status()}window.KDPProjectSafety={status,checkpoint};})();